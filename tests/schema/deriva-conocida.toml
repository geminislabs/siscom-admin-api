# Deriva conocida entre los modelos y el esquema de produccion
#
# La lee `scripts/verificar-deriva.py`. No es una lista de cosas aceptadas: es
# la fotografia de lo que hoy no coincide.
#
# **Hoy esta vacia**, y conviene que se note: no significa que el fichero
# sobre, significa que no queda deriva conocida de nulabilidad ni de claves
# foraneas. Nacio el 23/09/2026 con 32 entradas y se drenó entera entre el 23 y
# el 24 — cuatro donde mandaba la base (#113), un 500 vivo que resulto no ser
# deuda de esquema (#114), y las 25 restantes en la `033`.
#
# COMO SE USA
# ===========
# - Aparece una divergencia que no esta aqui  -> la CI falla. O se arregla, o
#   entra en esta lista con su porque.
# - Una de aqui deja de ocurrir               -> la CI falla igual. Hay que
#   quitarla: una linea base que no se poda deja de describir el presente.
#
# Estando vacia, la primera regla es la unica que puede dispararse, y eso es
# exactamente lo que se quiere: a partir de ahora, cualquier columna que el
# modelo declare distinto de la base **se ve el mismo dia**.

nulabilidad = []

claves_foraneas = []
